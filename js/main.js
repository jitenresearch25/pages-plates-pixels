const menuToggle=document.querySelector('.menu-toggle');const navLinks=document.querySelector('.nav-links');if(menuToggle&&navLinks){menuToggle.addEventListener('click',()=>navLinks.classList.toggle('open'));}
const copyBtn=document.getElementById('copyMessageBtn');if(copyBtn){copyBtn.addEventListener('click',async()=>{const name=document.getElementById('visitorName').value.trim();const msg=document.getElementById('visitorMessage').value.trim();const status=document.getElementById('copyStatus');const text=`Name: ${name||'Anonymous'}\n\nMessage:\n${msg||''}`;try{await navigator.clipboard.writeText(text);status.textContent='Message copied. Now send it through Instagram or Facebook.';}catch(e){status.textContent='Copy failed. Please select the text and copy manually.';}});}
